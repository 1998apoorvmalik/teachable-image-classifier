import 'package:flutter/material.dart';

class AdjusterTool extends StatelessWidget {
  const AdjusterTool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Adjuster Tool'));
  }
}
