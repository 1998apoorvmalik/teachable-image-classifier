import 'package:flutter/material.dart';

class CropTool extends StatelessWidget {
  const CropTool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Crop Tool'));
  }
}
